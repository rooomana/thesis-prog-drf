name: Run scripts

on:
  push:
    branches:
      - main

jobs:
  run_scripts:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

#      - name: Set MATLAB Path
#        run: |
#          $MATLAB_PATH = "C:\Program Files\MATLAB\R2024b\bin"
#          $env:Path += ";$MATLAB_PATH"

#      - name: Verify MATLAB Path
#        run: |
#          echo "Current working directory:"
#          Get-Location
#          matlab -help

      - name: Set up MATLAB
        uses: matlab-actions/setup-matlab@v1

      - name: Run MATLAB script 1
        working-directory: ./Matlab
        run: |
          echo "Current working directory:"
          Get-Location
          matlab -batch "run('Main_1_Data_aggregation.m')"

      - name: Run MATLAB script 2
        working-directory: ./Matlab
        run: |
          echo "Current working directory:"
          Get-Location
          matlab -batch "run('Main_2_Data_labeling.m')"

      - name: Run Python script
        working-directory: ./Python
        run: |
          echo "Current working directory:"
          Get-Location
          python Classification.py
